# src/test/resources/application-test.yml
spring:
  profiles: test

  cloud:
    openfeign:
      client:
        config:
          USER-SERVICE:
            # автоматически подхватит порт, на котором запустился WireMock
            url: http://localhost:${wiremock.server.port}
