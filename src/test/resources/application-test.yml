spring:
  profiles: test

  cloud:
    discovery:
      enabled: false
    eureka:
      client:
        enabled: false
        register-with-eureka: false
        fetch-registry: false
    openfeign:
      eager-load:
        enabled: false

feign:
  httpclient:
    enabled: false
  okhttp:
    enabled: false
